<% @books.each do |book| %>
          <%"#{book.id}. book " %>
          <font face="Times New Roman" size="5" color="#9932CC"> <%= book.name %> </font>  <br><br>
          <%= link_to 'Görüntüle', book %>
          <%= link_to 'Düzenle', edit_book_path(book) %>
          <%= link_to 'Sil', book, method: :delete, data: { confirm: 'Kitap silinsin mi??' } %>
          <%= button_to "Kitaplığıma Ekle", add_to_list_path(book.id)%>

          <br>

    <% end %>
<br>
<font face="Times New Roman" size="10"> <%= link_to 'Yeni Kitap', new_book_path %> </font>  <br><br>
